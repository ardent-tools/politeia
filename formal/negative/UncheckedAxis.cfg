\* Configuration for the dropped-axis module. CI requires TLC to REJECT this.

CONSTANTS
    Actions = {a1}
    Resources = {r1}
    Effects = {e1}
    DataClasses = {d1}
    Audiences = {u1}
    MaxExpiry = 1
    MaxCap = 1

SPECIFICATION Spec

\* Monotonic is listed deliberately: it is expected to HOLD here even though an
\* axis has been dropped, because it is defined in terms of the same weakened
\* rule that admits the children. Its passing beside a failing
\* EveryAxisIsChecked is the point being demonstrated.
INVARIANT Monotonic
INVARIANT EveryAxisIsChecked

CONSTRAINT GrantBound

CHECK_DEADLOCK FALSE
