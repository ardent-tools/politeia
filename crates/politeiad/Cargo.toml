[package]
name = "politeiad"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
anyhow.workspace = true
tokio.workspace = true

# WHY dev-only: the binary is still a scaffold and does not run the slice. The
# integration test does, which is the increment that can be honest today --
# depending on these from the binary would claim the daemon composes them when
# it does not.
[dev-dependencies]
jiff.workspace = true
politeia-core.workspace = true
politeia-evidence.workspace = true
politeia-policy.workspace = true

[lints]
workspace = true

[package.metadata.kanon]
maturity = "scaffold"
since = "2026-08-19"
exit-criteria = "The daemon demonstrates the complete docs/18 first vertical slice under adversarial tests with every trusted component explained"
